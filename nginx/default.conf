server {
    listen 80;

    root /usr/share/nginx/html;
    index index.html;

    # Handle all SPA routes by redirecting to index.html
    location / {
        try_files $uri $uri/ /index.html;
    }

    # Optional: Handle error pages (404 to index.html)
    error_page 404 /index.html;
}